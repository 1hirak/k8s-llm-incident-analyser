apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: demo
spec:
  template:
    spec:
      containers:
      - name: demo-app
        readinessProbe:
          httpGet:
            path: /does-not-exist
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
